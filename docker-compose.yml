version: "3.7"
services:
  speedlogger-web:
    image: brennentsmith/internet-speed-logger
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    restart: always
    volumes:
      - ./config:/data/config
  speedlogger-runner:
    image: brennentsmith/internet-speed-logger
    depends_on:
      - speedlogger-web
    restart: always
    entrypoint:
      - node
      - run-speedtest.js
      - daemon
    volumes:
      - ./config:/data/config
  mongo:
    image: andresvidal/rpi3-mongodb3:latest
    expose:
      - "27017"
    # These environment things don't currently work
    environment:
      - MONGODB_USERNAME=speedtest
      - MONGODB_PASSWORD=speedtest
      - MONGODB_DATABASE=speedtest
    volumes:
      - ./mongo-persistence:/data/db/
